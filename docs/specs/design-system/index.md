# Design System

## Overview

The design system defines Liebe's default visual language: a token contract every component reads, a shared card anatomy, a domain color discipline, and size-adaptive card layouts. It is the foundation the [theming system](../theming/) overrides and the [component workshop](../storybook/) exercises.

**Status: implemented.** [0010](../../changes/0010-design-tokens-and-anatomy.md) delivered the whole of this spec bar one section: the [token contract](#token-contract) is declared for both appearances and exercised in the workshop (PR 1); the [card anatomy](#card-anatomy) parts — icon circle, meta block, pills, chips, big value, sparkline (PR 2) and the embedded slider in both orientations (PR 3) — carry their stable classes and attributes; and the card shell is a token-styled `liebe-card` tile (PR 4), which is also where the cards' inline visual declarations went away and the shell stopped being a Radix `Card variant="classic"`. Every existing card takes its **shell, icon circle, name and state line** from the anatomy, so those parts are reachable by the [stable selector contract](../theming/index.md#stable-selector-contract) on every card; each card's own interior — the thermostat's dial, the weather forecast rows, the camera's stream overlay — is still bespoke, and moving those onto the anatomy (or deciding they are content, which the domain-colour rule exempts) is the per-card wave that follows. The [size-adaptive layout tiers](#size-adaptive-layouts) are [0011](../../changes/0011-layout-tiers.md)'s and are now shipped end to end: its PR 1 retired the legacy `size` prop, derived each card's tier from the effective span the grid lays it out at and stamped `data-tier` on every shell, and PRs 2 and 3 implemented the per-tier content across every existing card — so both the boundary table and the per-tier content rules below describe what renders. `CameraCard` was the one card 0011 left stamped but not consuming, because its degradation below 2×2 is behavioural (stream unmount, still thumbnail, lazy-mount fullscreen); [0021](../../changes/0021-camera-presentation-options.md) implemented that behaviour, so no card is exempt any more. No card now keeps a control the `glance` table would drop: the four non-boolean input helpers gave theirs up when [0022](../../changes/0022-switch-input-helpers-to-spec.md) registered their dialog controls, and the thermostat — the last of them — gave up its setpoint stepper when [0017](../../changes/0017-climate-card-to-spec.md) registered its own in the same PR; the per-tier _controls_ each option doc specifies beyond what exists today arrive with the per-card wave (0016+). The design was validated in a throwaway static mockup which this spec supersedes and replaces as the sole reference.

**Implemented is not the same as met.** Five defects were opened against rules this status calls shipped, now tracked as change documents rather than issues, and the flip closed none of them; a sixth was measured while closing the third and is listed below — the token contract now being themeable is exactly what made the glyph-on-tint one measurable on every theme rather than only on Default. All five are now closed: the domain text tokens missing AA on the light card surface, which [0035](../../changes/0035-light-appearance-contrast.md) fixed by pinning the Default theme's `-text` companions per appearance, so all ten hues now clear 4.5:1 on the card surface in both; the glyph on its own tint, which the same change fixed by making the [active-state pattern](#domain-color-discipline)'s glyph step per appearance; the two remaining unnamed controls, which it named; the touch floor below, kept in the list rather than deleted because the reason it was misdiagnosed for as long as it was is worth keeping; and text over condition artwork, which [0030](../../changes/0030-weather-forecast-legibility.md) closed with a scrim on both consumers of the rule. The sixth is closed too, by the same change's PR 4, which is the one that split what a live hue may colour from what it may not — and that PR's measurement opened a **seventh**, listed last: the split reaches the two themes that let the pattern paint the glyph, and LCARS paints it itself.

- The domain glyph on its own 20% tint missed 3:1 in light, on every theme — the pattern as calibrated, not a single theme's choice — and is **closed by [0035](../../changes/0035-light-appearance-contrast.md) PR 2**, which gave the pattern a per-appearance glyph step. Every hue on every built-in theme now clears the floor in the appearances that theme provides — worst 3.68:1, was 1.27:1 — measured from decoded pixels per hue per theme; LCARS is dark-only and owns its icon treatment outright, so its one figure is unchanged, and dark is byte-identical everywhere — this fix was scoped to **light**, which is where the defect was. What remains open of [0035](../../changes/0035-light-appearance-contrast.md) is this same pattern's two unfinished corners, and neither is the live-hue defect below, which its PR 4 has since closed. `media` misses the glyph floor in **dark** on its own tint — 2.81:1, the only hue of the ten that does, found when [0033](../../changes/0033-icon-only-cards.md) reproduced this exact composite at tile scale — and is 0035's PR 5. LCARS's own treatment of a data-driven hue is the last bullet here, and is its PR 6.
- The naming residue is **closed by [0035](../../changes/0035-light-appearance-contrast.md) PR 3**: the two icon-only controls that still rendered anonymous — `InputSelectCard`'s select trigger and the switch `InputBooleanCard` renders in its own tile at `controlStyle: switch` — now carry names, so the naming rule under [card anatomy](#card-anatomy) is held everywhere the workshop can see. The anatomy migration had closed the rest of that audit as a side effect, and the residue was named here rather than counted so the claim stayed checkable; the count is now the evidence. Re-run over all 645 stories in **both** appearances (1290 runs, none skipped), axe reports **zero** `button-name` nodes, down from 32 on 15 stories — ten `input_select` card stories, four theme-gallery stories that place one, and the one `input_boolean` story that configures the switch. `aria-input-field-name`, the other half of the original audit, is zero too. What the same run still reported was `color-contrast`, on stories that change did not touch: 32 of its 71 nodes were the person card's initials on their own identity tint — the live-hue defect below, and the largest single group in the remainder. **PR 4 has since closed those 32**, confirmed story for story by a re-run; that bullet also records why its own residual figure is not comparable with the 71 here.
- The 44px touch floor is enforced on Radix controls again — **closed by [0036](../../changes/0036-theming-contract-gaps.md) PR 1**, which moved the vendored sheets into a layer below the baseline. Being layered was never what was missing: sharing `liebe-base` was, because inside one layer Radix's class selectors outranked the element selectors the floor is written with ([theming — application mechanism](../theming/index.md#application-mechanism)). Measured under an emulated coarse pointer rather than reasoned about. The rest of the cascade gaps stay with 0036.
- A **live hue landing on its own tint** — a light card under `useLightColor` drawing the glyph in the bulb's colour on a 20% tint of that same colour, and a person avatar drawing its initials in the identity colour on a 20% tint of that — is **closed by [0035](../../changes/0035-light-appearance-contrast.md) PR 4**, which split what a data-driven hue may colour from what it may not: it supplies the surface and solid roles and no foreground one, so the glyph and the text role take the neutral foreground instead (the [active-state pattern](#domain-color-discipline) below states the rule). It could not be fixed by choosing a step, which is what made it a separate task from the pattern's own glyph fix: a live hue has no darker step to reach for, and lowering the tint's alpha moves the ground towards the card and makes the figure worse. Measured from decoded pixels on Default and Liquid Glass in both appearances, nested roots included: the bulb glyph was **1.01:1** at worst and is **5.95:1** at worst against a 3:1 floor, and the avatar's initials — which are text, at 4.5:1 — were **2.19–4.08:1** for every one of the eight identity hues and are now **10.53:1** at worst. LCARS is byte-identical either way, because it declares the glyph's colour on the part itself and reads neither role; its own treatment of a live hue is noted with the pattern below. The axe re-run corroborates it and is the checkable half: over all 658 stories in both appearances (1316 runs, none skipped, both passes taken from the same build), `color-contrast` falls from 51 nodes on 24 stories to **19 on 8**, and the 32 nodes removed are exactly the 2×16 person-card nodes the PR 3 audit had attributed to this defect. That run's own **71** is not comparable with the 51 above, and the gap is sampling rather than drift: it read each story 150 ms after render, inside the anatomy's 280 ms colour transition, so an animated tile was scored at a colour it holds in neither state. Sampled the same way, this build reports 37–40 — which is 71 − 32 to within that timing's own run-to-run spread — and settling the transitions first is what takes it to 19.
- Weather card text over its condition artwork is **closed by [0030](../../changes/0030-weather-forecast-legibility.md) PR 1**, which put a real scrim under it and moved the colour onto the scoped foreground tokens. Measured from painted pixels in Chromium, across all ten shipped artworks, all four tiers, the three artwork-bearing variants and an adversarial all-white image: worst 5.18:1, was 1.01:1. The reference implementation was measured in the same pass and was the worse of the two — its scrim painted over the text rather than under it, so the media card's own worst was 1.00:1; it now measures 7.11:1 against a white cover. The recorded 2.64:1 was a kinder reading than the worst pixel of the artwork actually supports.
- **LCARS puts a fixed dark glyph on a live hue**, and misses both floors where the data is dark: measured **2.44:1** for a pure-blue bulb against the 3:1 glyph floor, and **3.90:1** for the worst of the eight identity hues against the 4.5:1 the avatar's initials answer to (the other six clear it). It is the one place the live-hue rule under [domain color discipline](#domain-color-discipline) does not reach, and by construction rather than by omission: the theme declares `color` on the part, so it reads no role token, and its treatment is a solid disc of the hue rather than a tint of it — a different composite that no repin of the base pattern touches. Found while measuring [0035](../../changes/0035-light-appearance-contrast.md) PR 4 and tracked as that change's sixth task. LCARS being dark-only, this is a dark-appearance figure throughout.
- **[Cross-axis fit](#cross-axis-fit) was the one section below that was target behaviour rather than a description of what renders, and is no longer.** Change [0042](../../changes/0042-tall-tile-control-geometry.md) PR 1 settled it as a contract and its PRs 2, 3 and 4 made the cards hold it; **all four have landed**, so this section now describes what renders: `input_number` gives way to the vertical slider at `tall` instead of rendering a stepper past the tile's edge, and every `tall` vertical slider now takes the content region where the token does not fit and is omitted below either floor. PR 4 measured the remaining parts and settled each of them — see "Every part this rule reaches has now been measured" below for which route each took.

The design targets what the smart-home community demonstrably prefers: flat, shadowless cards one elevation step above the background in dark mode, large radii, a tinted icon-circle state pattern, strict domain-color discipline, and two-line typography. All hue on the dashboard carries state meaning; chrome stays neutral.

## Background

Research across card-ecosystem popularity data, community forums, and the most-shared dashboards converged on a consistent visual grammar for "polished" Home Assistant dashboards (dark-first, 20px radii, icon-in-tinted-circle, embedded sliders, chip rows). Liebe's defaults adopt that grammar so dashboards look considered without any user styling. Radix UI Themes remains the component substrate: the token contract aliases Radix tokens wherever a close match exists so appearance switching and Radix upgrades keep working.

## Requirements

### Token contract

- The system MUST define all visual attributes as CSS custom properties (the "token contract") on the panel's themed root — the element carrying Radix's theme scope, see [Design](#design) — under a `--liebe-*` namespace.
- Components MUST read visual attributes only through tokens (directly or via a class that reads tokens). Components MUST NOT hardcode colors, radii, blur, borders, or shadows that a theme could reasonably want to change.
- Where a Radix Themes token is a close match, the Liebe token MUST alias it (e.g. `--liebe-fg: var(--gray-12)`) rather than duplicate a literal value, so Radix appearance switching flows through automatically.
- The token contract is the public theming API: token names, meanings, and value types MUST be documented in this spec and MUST NOT be renamed without a migration note in the [theming spec](../theming/).

Geometry tokens and defaults:

| Token                          | Default  | Purpose                                                                                               |
| ------------------------------ | -------- | ----------------------------------------------------------------------------------------------------- |
| `--liebe-card-radius`          | `20px`   | Card corner radius (accepts 1–4 value shorthand)                                                      |
| `--liebe-chip-radius`          | `9999px` | Chips and pill buttons                                                                                |
| `--liebe-control-radius`       | `12px`   | Embedded controls (sliders, mode pills, artwork)                                                      |
| `--liebe-circle-radius`        | `50%`    | Icon circles, avatars, round buttons                                                                  |
| `--liebe-card-padding`         | `14px`   | Card inner padding                                                                                    |
| `--liebe-grid-gap`             | `12px`   | Gap between grid cells                                                                                |
| `--liebe-icon-circle`          | `40px`   | Icon circle diameter (glyph ≈ 22px)                                                                   |
| `--liebe-control-height`       | `42px`   | Embedded slider thickness — a preferred maximum, not a fixed size ([cross-axis fit](#cross-axis-fit)) |
| `--liebe-chip-height`          | `34px`   | Chip height                                                                                           |
| `--liebe-card-min-height-row`  | `60px`   | Height floor for the single-row tiers (`glance`, `row`)                                               |
| `--liebe-card-min-height-tall` | `120px`  | Height floor for the multi-row tiers (`tall`, `full`)                                                 |
| `--liebe-graph-height-inline`  | `32px`   | Sparkline band sharing a line with other content                                                      |
| `--liebe-graph-height-dialog`  | `96px`   | History graph in the entity detail dialog                                                             |

The two graph tokens are the **fixed** graph heights, and the only ones: a graph that shares its line with other content, and the dialog's. The `full` tier's card graph is deliberately absent from this table because it carries no fixed height at all — it grows with the tile ([options/sensor — tier layouts](../entity-cards/options/sensor.md#tier-layouts), change [0031](../../changes/0031-sensor-graph-fill.md)). _Both tokens ship, defaulting to the literals they replaced, so a theme that leaves them alone sees no change._

**A geometry token is not a promise that the part measures that.** Several of these describe a part's size across an axis the tile may be narrower than, and the tile's width is the user's grid and the theme's inset rather than this spec's to pin — so where the two disagree the tile wins. [Cross-axis fit](#cross-axis-fit) below owns which parts that applies to and what the alternative is.

Surface tokens (dark defaults / light values):

| Token                 | Dark         | Light                        |
| --------------------- | ------------ | ---------------------------- |
| `--liebe-bg`          | `#111114`    | `#efeef2`                    |
| `--liebe-card-bg`     | `#1b1b1f`    | `#fcfcfd`                    |
| `--liebe-card-border` | `none`       | `none`                       |
| `--liebe-card-blur`   | `none`       | `none`                       |
| `--liebe-card-shadow` | `none`       | `0 2px 4px rgba(0,0,0,0.08)` |
| `--liebe-fg`          | `#f2f2f5`    | `#1a1a1e`                    |
| `--liebe-muted`       | 62% alpha fg | 60% alpha fg                 |
| `--liebe-faint`       | 38% alpha fg | 38% alpha fg                 |
| `--liebe-hairline`    | 7% alpha fg  | 8% alpha fg                  |
| `--liebe-track`       | 8% alpha fg  | 7% alpha fg                  |
| `--liebe-media-bg`    | `--gray-3`   | `--gray-3`                   |

These literal values are the design intent; the implementation SHOULD substitute the nearest Radix gray-scale tokens (`--gray-*`, `--gray-a*`) where the difference is imperceptible. **Resolved in [0010](../../changes/0010-design-tokens-and-anatomy.md) PR 1:** every surface token that carries a _color_ aliases a Radix token — no colour literal is pinned — but the alias is a **gray-scale step chosen per appearance**, not the semantic `--color-*` pair. (The structural values stay as the table specifies: `--liebe-card-border` and `--liebe-card-blur` are `none` in both appearances, as is `--liebe-card-shadow` in dark, since there is no Radix token for "no shadow".) In dark those coincide (`--color-background` → `--gray-1`, `--color-panel-solid` → `--gray-2`, both within ~2/255 of the reference); in light both semantic tokens resolve to plain white, which would erase the ground-to-card separation the design depends on, so light aliases `--gray-3` for the ground and `--gray-1` for the card. With the default gray (slate, paired with the default indigo accent) the steps match the reference hexes near-exactly in both appearances — `--gray-3` is `#f0f0f3` against a `#efeef2` reference, `--gray-1` is `#fcfcfd` exactly. The remaining surface tokens alias directly: `--liebe-fg: var(--gray-12)`, muted/faint/hairline/track the `--gray-a*` alpha steps, and the light card shadow `var(--black-a2)`. `--liebe-media-bg` is the well a picture sits in — a camera thumbnail or feed before the image paints, and the letterbox around one that does not fill its box (change [0021](../../changes/0021-camera-presentation-options.md)); it aliases the same `--gray-3` step in both appearances, so it flips with the scale like every other alias, and it exists so a camera tile's neutral is reachable from the theme layer rather than pinned to a Radix scale variable at the point of use.

Typography tokens:

| Token                    | Default                    | Purpose                                                |
| ------------------------ | -------------------------- | ------------------------------------------------------ |
| `--liebe-font-family`    | system stack (Radix)       | Typeface for all dashboard text                        |
| `--liebe-text-transform` | `none`                     | Casing applied to names, state text, labels, and chips |
| `--liebe-letter-spacing` | `normal`                   | Tracking companion to the casing token                 |
| `--liebe-font-numeric`   | `var(--liebe-font-family)` | Typeface for numeric readouts, so figures can differ   |

These MUST be declared on the themed root and inherited, not applied per component, so a theme that sets them restyles every text surface — including [portalled overlays](../theming/index.md#application-mechanism), whose mirrored root carries the same declarations. This is what makes a whole-dashboard typographic reskin (LCARS: bundled Antonio, uppercase, slight tracking) expressible as token values rather than as rules targeting selectors outside the [stable contract](../theming/index.md#stable-selector-contract). Casing MUST go through `--liebe-text-transform` rather than literal uppercase text or per-component `text-transform`, so the default theme's sentence case and a theme's uppercase are the same mechanism.

Part role tokens:

| Token                       | Default             | Where the default lives            | Purpose                                        |
| --------------------------- | ------------------- | ---------------------------------- | ---------------------------------------------- |
| `--liebe-part-color`        | `--liebe-c-default` | the root, re-declared on each part | The domain colour the part in hand resolved to |
| `--liebe-part-label`        | `--liebe-muted`     | a fallback where it is used        | Colour of a label on an inactive pill or chip  |
| `--liebe-part-label-active` | `--liebe-fg`        | a fallback where it is used        | Colour of a label on an active pill or chip    |

These three publish what the base layer already worked out about one part, and each answers a question a theme could otherwise only answer by restating something.

`--liebe-part-color` is published to be **read**, and the third column says why that is not the same as the others. It MUST re-resolve on every part that carries a `data-color`, so on any part it means _that_ part's own colour — including the live bulb colour under the `useLightColor` exception below, since a theme colouring a part by this token must not be told the triplet the bulb is standing in for. The consequence of re-declaring it per part is that the ordinary override route does not reach it: a value declared on the part is the value the part uses however the cascade went above it, so setting it on the root reaches only a subtree that resolved no domain colour, which is what the default in the table is for. **The supported way to change what a part's hue _is_ remains its [`--liebe-c-*` triplet](#domain-color-discipline)**, which this token resolves from — it is an output, and the triplet is its input. (A later layer may still declare it on a part-scoped selector and win, as any later layer beats the base one; that is not forbidden, it is simply not a token override and does not compose with the resolution.) Without it a theme wanting a part's own hue restates the whole ten-way `data-color` mapping, which is a copy that drifts the next time the palette gains a row.

The two label tokens exist because the [active-state pattern](#domain-color-discipline) deliberately does not colour a **label** on a part: the pattern's hue is calibrated for a glyph at 3:1 and a 12.5px label needs 4.5:1, which no end of a triplet clears on that triplet's own 20% tint. The neutral foreground is therefore correct for a tinted part, and is the default — expressed as a `var()` fallback at the point of use rather than as a value on the root, because the neutral it falls back to is one components resolve locally and an alias on the root would resolve it there and inherit the answer. A theme that fills the part **solid** with the hue instead has a different ground under the same label and needs a dark one on it — the treatment the [LCARS section](../theming/index.md#built-in-theme-lcars) specifies, which was unreachable while the two colours were literals behind internal class names. A theme MUST measure a label it recolours against the fill it recoloured it for, at 4.5:1, in each appearance separately: the same reasoning as the `-text` pins, and for the same reason — a ratio cleared on one ground is no evidence about another. Like every other override, both MUST be declared on [the root](../theming/index.md#configuration--selection): a token declared on a part instead defeats the layer model outright, because a value on a descendant wins over an inherited one regardless of which layer either came from — so a theme could put a label colour beyond the reach of the user layer that is supposed to have the last word.

#### Scenario: A theme changes one token and every card follows

- **GIVEN** the default theme with `--liebe-card-radius: 20px`
- **WHEN** a theme sets `--liebe-card-radius: 0`
- **THEN** every card, in every state and size, renders square corners with no per-component override needed.

### Domain color discipline

- Each entity domain MUST have a semantic color token; these are the only hue carriers in **dashboard chrome and UI accents** — icon tints, state text, control fills, selection states. Neutral chrome MUST stay gray-scale. **Content is exempt**: media artwork, camera feeds, weather condition imagery, person avatars (including the generated initials backgrounds, which are identity colors, not state), and whatever decorative color a theme brings are content/theming, not chrome, and carry whatever color they carry.
- Default mapping (Radix scale in parentheses — implementations MUST use the Radix scale, shown hex is the design reference):

| Token               | Domain / meaning                                                                          | Reference | Radix scale |
| ------------------- | ----------------------------------------------------------------------------------------- | --------- | ----------- |
| `--liebe-c-light`   | Lights on                                                                                 | `#ffc107` | `amber`     |
| `--liebe-c-heat`    | Climate heating                                                                           | `#ff6f22` | `orange`    |
| `--liebe-c-cool`    | Climate cooling, covers                                                                   | `#29b6f6` | `sky`       |
| `--liebe-c-ok`      | Locked, home, secure, fan                                                                 | `#4caf50` | `green`     |
| `--liebe-c-alert`   | Alerts, unlocked, away                                                                    | `#f44336` | `red`       |
| `--liebe-c-media`   | Media playing, scenes                                                                     | `#7986cb` | `indigo`    |
| `--liebe-c-vacuum`  | Vacuum active                                                                             | `#26a69a` | `teal`      |
| `--liebe-c-water`   | Humidity, water                                                                           | `#4fc3f7` | `cyan`      |
| `--liebe-c-default` | Generic active — switches, outlets, input helpers, and any domain without a dedicated row | `#2196f3` | `blue`      |
| `--liebe-c-brand`   | Liebe brand mark only                                                                     | `#e9526f` | `crimson`   |

- Every **rendered semantic state** MUST resolve to exactly one token — resolution is state-aware, not a fixed per-domain assignment: a thermostat resolves heat/cool/ok by HVAC state, a lock resolves ok/alert by lock state, binary sensors resolve by `device_class`; domains with no state-specific rule use their table row, and domains without any row use `--liebe-c-default`. This state-aware resolution is what the universal `color: auto` option means ([entity-cards options — common](../entity-cards/options/common.md)), and it is total: every card, in every state, including the unmapped-domain fallback, resolves to a defined token. **One documented exception:** the light card's `useLightColor` option MAY tint the active treatment with the bulb's actual RGB color under `color: auto` ([options/light](../entity-cards/options/light.md)) — real light color is information no fixed token can carry; every other card resolves to tokens only, and an explicit named `color` overrides the bulb color too. What a data-driven hue may colour once it is through — this one, and the person avatar's identity colour, which the content exemption above covers — is the live-hue rule under the active-state pattern below.

- **Each domain color is a token triplet**, so a theme remapping one hue remaps the entire active treatment: `--liebe-c-<name>` (the base — saturated glyph/solid role), `--liebe-c-<name>-tint` (the ~20%-alpha surface role), and `--liebe-c-<name>-text` (the text role at readable contrast). The **base layer** MUST define the companions only as derivations from the base token — tint as `color-mix(in srgb, var(--liebe-c-<name>) 20%, transparent)`, text as `var(--liebe-c-<name>)` — never as fixed color values, so a theme that remaps only the base automatically recolors glyph, tint, and text together (a fixed alias like `var(--amber-a4)` in the base layer would survive a base-only remap and break this promise). The **default theme** then explicitly sets the base tokens to their Radix step-9 colors and pins all ten `-text` companions per appearance (raw step-9 hues lack text contrast; the steps are in the next bullet). An explicit companion is an override like any other: wherever the active theme has set one, the automatic base-only-remap promise applies only to the tokens it left derived — so user CSS remapping a base **under the Default theme** SHOULD also set `-text` (the tint still follows automatically via derivation, since Default does not pin tints); a theme, for its part, MUST pin `-text` outright (next bullet; contract note in [theming](../theming/index.md#stable-selector-contract)).
- **The derived `-text` default is not usable as rendered: every theme MUST pin all ten `-text` tokens.** The derivation resolves `-text` to the domain base — a raw Radix step 9 — which is calibrated as a saturated glyph/solid hue and carries no text contrast on any ground, in either appearance. A theme that remaps the bases and leaves `-text` derived therefore ships unreadable state text, and nothing catches it: the derivation is what keeps a base-only remap coherent, so there is no invalid value to detect and no gate that fails. This is why Liquid Glass pins step 12 throughout and Default pins `-text` explicitly per appearance rather than inheriting the derivation. **A pin MUST clear the 4.5:1 text floor against the card surface in each appearance separately**, because a Radix step 11 is calibrated against steps 1–2 of its own scale rather than against a neutral: the same step measures differently on the light card surface than on the dark one, so clearing the floor in one appearance is no evidence for the other. Default consequently pins step 12 in **light** appearance for the three hues whose step 11 measures short there — `--liebe-c-light` (amber), `--liebe-c-heat` (orange), `--liebe-c-vacuum` (teal) — and step 11 for the other seven in light and for all ten in dark; darkening a hue that already clears the floor is a visual change to every card of that domain, not part of the remedy. The same pin carries the glyph on the tint in light appearance — see the active-state pattern below, which reads the `-text` token there rather than declaring a step of its own. Pinning is the theme's job, not the base layer's.
- **Active-state pattern**: an active element MUST render its glyph in the domain color on the domain's tint token, at a step that clears the 3:1 non-text floor **against that tint**; an inactive element MUST render a muted glyph on a ~5%-alpha neutral (`--gray-a3`). This single pattern MUST be used consistently by icon circles, chips, selected pills, and slider fills.
- **The glyph step is per appearance, and the light one is the `-text` token.** The tint is a 20% veil of the base hue, so what the glyph sits on moves with the appearance: over the dark card the veil stays dark and the saturated base step reads against it, but over the near-white card it comes out a pale wash of the base and the base step cannot reach 3:1 against it — measured 1.40:1 for amber and 2.50:1 for green under Default, and within noise of that under Liquid Glass, which is what settled it as the pattern's calibration rather than a theme's colour choice ([0035](../../changes/0035-light-appearance-contrast.md)). Two of the pattern's three knobs are dead ends: lowering the tint's alpha moves the ground **towards** the card and makes the figure worse, and a border on the circle bounds the shape without touching the glyph. So the glyph step is the knob, and in **light** appearance the pattern MUST render the glyph in the domain's `-text` token; in **dark** it MUST render the base. Reusing `-text` asks no theme for a fourth token: all ten hues clear the floor on all three built-in themes at that step, by a worst measured margin of 3.68:1. It is **not** a guarantee that follows from the text pin, and MUST NOT be treated as one — the two floors are measured against different grounds, and a near-neutral hue can clear 4.5:1 on the card while its own 20% tint sits close enough to the text step to miss 3:1. **A theme MUST measure the glyph against the tint as well as the text against the card**, and a theme whose hues fail that way owes itself a darker glyph, not an exception. The consequence for user CSS under a theme that pins `-text` — which is every theme — is that a base-only remap now moves the tint and the dark glyph but not the light glyph, which is the same reason such a remap SHOULD set `-text` too ([theming — stable selector contract](../theming/index.md#stable-selector-contract)).
- **A live hue colours the surface and the solid, never a foreground.** Where a data-driven colour stands in for a triplet — the two paths the exceptions above grant, a bulb's own RGB and a person's identity colour — it MUST supply the tint and the saturated solid role and MUST NOT supply the glyph or the text role; those take the neutral foreground (`--liebe-fg`). The pattern's own remedy is unavailable to a live hue and that is the whole reason this is a separate rule: the glyph step above picks a darker step of a scale, and a live colour is data with no scale behind it, while the other two knobs are the dead ends already named. A hue drawn on a 20% veil of itself is the degenerate case the floors exist to exclude — measured **1.01:1** for a bulb reporting white and **2.19:1** for the worst identity hue, in light and dark alike ([0035](../../changes/0035-light-appearance-contrast.md) PR 4) — and the information survives the split, because the surface is what carries it: [options/light](../entity-cards/options/light.md#light-color-theming-uselightcolor) granted the bulb colour "the icon-circle tint and the slider fill" and never the glyph. This is the same move the anatomy already makes for every other foreground that lands on a tint of its own hue — a pill or chip label, a slider readout — and a theme that fills a part with a saturated hue instead of tinting it owes its own foreground, exactly as it does there. LCARS is that theme: it paints an active circle as a solid disc of `--liebe-part-color` with a fixed dark glyph, which reads against a domain step 9 and, on a live hue, is whatever the data makes it — measured worst **2.44:1** for a pure-blue bulb and **3.90:1** for an identity hue. A fixed foreground on an arbitrary colour cannot be measured into conformance, and no token repin reaches it; it is the theme's own treatment to answer for, and is in the outstanding list at the top of this spec rather than in this rule.
- State text ("On", "Heating", "Locked") SHOULD take the domain's text token; supporting values ("· 80%") stay muted.

#### Scenario: Light turns on

- **GIVEN** a light card whose entity is `off` (gray glyph on 5% neutral circle)
- **WHEN** the entity becomes `on`
- **THEN** the icon circle animates (≤300ms) to an amber glyph on 20% amber tint, and the state text "On" renders in the amber text step.
- **AND** the glyph clears 3:1 against that tint in both appearances — which in light appearance means the glyph renders in the amber text step too, the base step measuring 1.40:1 there.

### Typography

- Text MUST read its typeface, casing, and tracking from the typography tokens above rather than declaring them per component. The **default theme** sets `--liebe-font-family` to the system-native stack (Radix default) with `--liebe-text-transform: none`; no webfont ships in the default theme.
- Type ramp (all MUST be expressed via tokens or the Radix size scale):
  - Entity name: 14px / 500 / 20px line-height, single line, ellipsized.
  - State line: 12.5px / 400, muted color, single line, ellipsized.
  - Large numeric readout (sensor value, target temp): 27–34px / 300, `tabular-nums`.
  - Section/screen title: 24–26px / ~480, slight negative tracking.
  - Eyebrow labels: 11px / 650 / uppercase / 0.09em tracking, faint color.
- Numeric displays MUST use `font-variant-numeric: tabular-nums`.

### Card anatomy

Every entity card composes from a fixed set of anatomy parts, each with a stable, documented class name (`liebe-` prefix) so themes and tests can target them (see [theming — stable selectors](../theming/index.md#stable-selector-contract)):

- **Tile** (`liebe-card`): the card surface — `--liebe-card-bg`, `--liebe-card-radius`, `--liebe-card-border`, `--liebe-card-blur`, `--liebe-card-shadow`, padding `--liebe-card-padding`, `overflow: hidden`.
- **Icon circle** (`liebe-icon`): 40px circle implementing the active/inactive tint pattern.
- **Meta block** (`liebe-name`, `liebe-state`): the two-line name/state stack.
- **Embedded slider** (`liebe-slider`): a `--liebe-control-height` track — 42px by default, and the thickness the control _prefers_ rather than one it always has ([cross-axis fit](#cross-axis-fit)) — with a translucent domain-tint fill and a 3px saturated leading edge; supports horizontal and vertical orientation; value readout inside the track. In **both** orientations the fill MUST cover the track's full cross-axis — a vertical fill spans the track's whole width exactly as a horizontal fill spans its whole height (this held only for horizontal until change [0028](../../changes/0028-slider-rendering-fixes.md), which records the defect and its mechanism). A vertical slider MUST also render horizontally centred within whatever region hosts it, not pinned to the region's leading edge.
- **Pill controls** (`liebe-pill`): 38px equal-width mode buttons; selected pill uses the active tint pattern.
- **Chip** (`liebe-chip`): 34px pill for header rows — icon-dot + label, same tint pattern.
- **Big value** (`liebe-value`): the large numeric readout with unit in muted 14px.
- **Sparkline** (`liebe-spark`): inline history graph — 2px domain-color line, 14%-alpha area fill, emphasized endpoint dot; no axes or gridlines at card sizes. A `bar` variant draws one domain-colored column per point from a **zero baseline** instead, for series whose points are quantities per interval rather than levels (a counter's per-bucket differences, [options/sensor](../entity-cards/options/sensor.md) `graphMode`); zero is forced into the domain because a bar's length is its value, and a signed series therefore hangs its negative columns below the baseline. The line's endpoint dot and area fill are the line's alone. A series with no range to draw — every bar zero — falls back to the placeholder baseline, as an undrawable line does.

Every part MUST also stamp the attributes of the [stable selector contract](../theming/index.md#stable-selector-contract), which owns their names and semantics — including how a part's colour is selected.

- Every slider MUST expose a meaningful accessible name **on the element carrying `role="slider"`** — not on a wrapper around it. A name on the wrapper looks like a label in the markup while leaving the control anonymous to assistive technology, which is how the cards' sliders failed `aria-input-field-name` ([#192](https://github.com/fx/liebe/issues/192)). No rendered slider may be without one, and the name MUST identify what the control adjusts: a plausible stand-in ("Slider", "Value") satisfies the audit and tells a screen-reader user nothing, so it does not satisfy this rule.
- Text inside a tinted part (chip and pill labels) MUST NOT take the tint pattern's glyph colour. The pattern is calibrated for a glyph — a large graphical object at 3:1 — while a ~12.5px label needs 4.5:1, which neither the base hue nor its text step reaches on the 20% tint in light appearance. The hue is carried by the surface and the glyph; labels take the neutral foreground (muted when inactive).
- **Text over content imagery sits on a scrim.** Wherever card text or glyphs render over photographic or artwork content (media artwork, weather condition imagery), the card MUST interpose a darkening scrim between image and text sufficient to hold the 4.5:1 text floor against **any** image — the scrim is what makes the floor image-independent. Text shadows alone are NOT a scrim: they trace the glyph rather than establish a ground, and are how the weather card measured 2.64:1 over its own artwork ([#215](https://github.com/fx/liebe/issues/215)). Whatever produces the separation, overlaid text MUST stay reachable by the theme layer — a treatment that pins text colours outside the token contract trades one defect for another. The media player's backdrop is the reference implementation, and the reference is the structure rather than any particular strength: its own darkening had never been measured, and when it was it missed on both counts — the layer painted over the text rather than under it, and its top stop was a 25% wash. Both consumers were brought to the floor together by change [0030](../../changes/0030-weather-forecast-legibility.md) PR 1. Two mechanisms are named because the observable phrasing does not distinguish a working implementation from a failing one: the scrim MUST paint **beneath** the text (a translucent layer over both text and ground darkens both and separates neither, which measured 1.01:1 while looking like a scrim in the markup), and the foreground override MUST reach the **parts**, which resolve `--part-text` from their own `[data-color]` rule and therefore never see a value inherited from the tile.
- **Icon-only tiles carry the state on the tile itself.** Everywhere else the tile stays neutral — hue lives in the icon circle and the other tinted parts — but a card in the universal icon-only presentation ([entity-cards options — common](../entity-cards/options/common.md#icon-only-presentation-icononly), change [0033](../../changes/0033-icon-only-cards.md)) has surrendered every part that could carry state, so the tile becomes the tint surface: active renders the domain's `-tint` token over the card surface with the glyph at the step the [active-state pattern](#domain-color-discipline) gives it — the base hue in dark, the `-text` token in light, since this is that pattern at tile scale and the 3:1 floor does not move with the size of the tint — inactive renders the standard ~5%-alpha neutral with a muted glyph, with the usual ≤300ms transition. The resolution feeding it is the same state-aware `color: auto` chain every card uses (including the light card's bulb-colour exception under `useLightColor`), and a level-bearing active entity (brightness, fan speed, cover position) SHOULD modulate the tint's strength with its level so a dimmed lamp reads dimmer than a full one. This is one of exactly **two** exceptions to the neutral-tile rule — the other is the [background slider placement](#background-slider-placement) below, whose tint fill is likewise the state surface (and which supersedes this one when both are configured) — and it does not extend to danger states: the danger floor removes icon-only presentation entirely rather than negotiating with it.
- Cards MUST be flat in dark appearance (no border, no shadow) and MAY carry the small shadow token in light appearance.
- Card state (selected, error, unavailable, loading) MUST be carried on the tile as an `outline`, never a `border`: the border is pinned to `--liebe-card-border` for the theme's use, and an outline takes no layout space, so a card does not resize as its state changes. Selection and error are the two states that MAY carry hue, through `--liebe-c-default` and `--liebe-c-alert` respectively; error outranks selection at equal specificity. An **unavailable** card MUST be marked neutrally — a 1px dotted `--liebe-faint` outline — and MUST NOT be dimmed with `opacity`: halving the surface contrast drops the name and state lines to ~3:1, under the 4.5:1 floor, so a dimmed unavailable card is less legible than an undimmed one rather than more informative.
- Whole-card touch targets: the primary action MUST be the full tile; discrete controls MUST be ≥44px in at least one dimension (40px circles get ≥44px hit areas via padding).
- Press feedback: cards SHOULD scale to 0.98 on active press (coarse pointers), except cards that open in-place overlays (see camera constraint in [entity-cards](../entity-cards/)).

#### Scenario: Vertical slider fill spans its track

- **GIVEN** an `on` dimmable light at 50% rendered at 1×3 (`tall`), whose card body centres its content
- **WHEN** the slider renders vertically
- **THEN** the fill's box is exactly as wide as the track's content box, flush with both its edges and nothing clipped at the track boundary; and the slider itself sits horizontally centred in the region hosting it rather than against its leading edge.

#### Cross-axis fit

A tile's **content region** is the width the grid lays it out at, less `--liebe-card-padding` on both inline sides. At `tall` the tile is one column wide by definition, so that region is the narrowest the design system ever hands a control, and **it has no lower bound this spec can pin**: the column count is the user's (a screen stored at 16 columns is honoured at both desktop breakpoints), the container width is Home Assistant's, and the inset is the theme's. The three compose, and they compose past zero — a 16-column screen in a 960px container lays out a 43px tile, which LCARS's 44px of inline inset leaves with no content region at all.

The geometry tokens were never reconciled with any of that, which change [0042](../../changes/0042-tall-tile-control-geometry.md) measured and which these rules settle. They bind the parts laid out **inside** that region — everything the tier arrangements place. [Background slider placement](#background-slider-placement) is outside them by construction: its track is the tile edge to edge with no inset between the two, so it has no content region to overflow, consumes no layout space, and renders in every tier including `glance`. A theme's inset cannot shrink it and the floors below never omit it.

For everything else:

- **A part MUST NOT rely on the tile's inset to absorb an overhang.** The inset is a theme's to set and the tile clips (`overflow: hidden`), so a part sized past its content region stays uncropped only for the padding values its author happened to check — the default 14px leaves a 42px control 10.5px of clearance and LCARS leaves it 0.5px. "It fits inside the padding" is a property of one theme, never of the design system.
- **A control rendered at a one-cell-wide tier MUST be cross-axis flexible**: it takes the content region's width, and its geometry token names the size it prefers rather than the size it always has. `--liebe-control-height` is exactly that for the vertical slider — the track is 42px where the region affords it and the region's width where it does not.
- **Flexibility has floors on both axes, and a control under either MUST be omitted rather than rendered.** Cross-axis the floor is **24px** ([WCAG 2.2 SC 2.5.8](https://www.w3.org/TR/WCAG22/#target-size-minimum)); along its long axis it is the **44px** the touch floor already asks of every discrete control. Both are measured on the control **as it renders**, not inferred from the tile: `--liebe-card-min-height-tall` floors the tile, and the inset, the icon circle, the meta block and the gaps between them all come out before the control is given its band, so a tile that clears 120px can still leave a band that does not clear 44px. A control too thin, or too short, to land on is the same defect as a clipped one wearing a different symptom.
- **Neither floor may be checked by measuring the DOM.** Cards consume what the renderer and the shell hand them — tier, effective span, and the content width the shell observes ([size-adaptive layouts](#size-adaptive-layouts)) — and a capacity a card cannot derive from those is one the design system owes it as another such signal, never one the card measures for itself. **The two signals that debt comes to are the content width and the control band's height**, and they are owed for different reasons: the width is the cross axis and the shell already publishes it, while the long axis is the band the tier gives the control — the tile's height less the inset, the icon circle, the meta block and the gaps, none of which a card knows and no arithmetic on tier and span recovers. The design system's own layout component owns that band and publishes its height through the same shared observation, which is the signal being supplied rather than the prohibition being bent: one implementation, shared by every card, measuring a box the cards do not own. A band published as unobserved means "not measured" and renders, exactly as an unobserved width does — a control is omitted on a capacity that was measured and fell short, never on one nobody took. **Those two signals do not cover every capacity, and one is still open**: a vertical control forced onto a **row line** ([shared slider placement](../entity-cards/options/common.md#shared-slider-placement-sliderplacement)) is bounded by what the line has left once the icon circle and the gaps come out of it, and nothing publishes that — the content width is a coarse gate there rather than the exact capacity, so such a control may render thinner than the region suggests. It cannot be clipped, because the line's slot is cross-axis flexible and narrows with it, which is why this is an open refinement and not a defect of the same class as the two the floors close.
- **A control that cannot be sized down at all MUST be omitted at that tier**, in favour of one that can — never clipped, never scrolled. A control bounded by its content on every axis, such as a row of buttons, is in that class by construction: sizing it down has no floor to stop at short of its own contents. Substituting one control for another at a tier is the degradation `input_select`'s pills already make outside `full` ([options/input-helpers](../entity-cards/options/input-helpers.md)), not a new mechanism.
- **These floors outrank the no-last-control rule, and do not suspend it.** [Size-adaptive layouts](#size-adaptive-layouts) says a tier MUST NOT remove the last way to operate an entity, and requires a card to keep its minimal embedded control where the replacements do not exist yet — but a control under either floor is not a way to operate the entity, so keeping it satisfies the rule's letter and defeats its purpose. Where no control clears the floors, the card MUST omit them all and the entity's reachability MUST be carried by the tile's own primary action and the detail dialog's controls, exactly as `glance` carries it. A card whose domain has neither is the case the no-last-control rule is really about, and it MUST NOT be given a sub-floor control as the answer.

**Migration note for themes.** `--liebe-control-height` keeps its name and its 42px default; what changes is that setting it states a maximum. A theme MUST NOT assume a slider measures that value at `tall`, and cannot obtain a thicker track there from this token alone — the content region is what bounds it. A theme whose inset is large enough to drive that region under 24px is choosing omission over a control, which is why LCARS's `tall` sliders disappear on a 12-column desktop grid rather than rendering at 19px.

**Every part this rule reaches has now been measured**, which change [0042](../../changes/0042-tall-tile-control-geometry.md)'s fourth task did against the content region a 12-column desktop grid at 960px produces: a 63px tile, 35px of region, and 19px of it under LCARS. The measurement settled them by three different routes, and the routes are the rule working rather than an inconsistency:

- **Made cross-axis flexible.** The vertical slider's track takes `--liebe-control-height` where the region affords it and the region's width where it does not (PR 3). The **icon circle** joins it: `--liebe-icon-circle` is 40px, so a fixed circle overhung a 35px region by 2.5px per side and stayed uncropped only for the padding its author happened to check. Its arithmetic is not the slider's and is worth recording — a symmetric inset cancels, so the circle is _clipped_ only where the **tile itself** is narrower than the circle (a 16-column screen at 900px lays out a 39px tile) and merely overhangs its region at every width between. `min()` closes both, and an aspect ratio keeps a narrowed circle circular rather than trading the overhang for an ellipse.
- **Omitted, because they cannot be sized down at all.** The `input_number` stepper (PR 2), and now `input_text`'s field and `input_datetime`'s inputs: 100–200px of inline minimum width, cropped by the tile rather than merely overhanging it, and bounded by their own content on exactly the axis this tier is one cell wide. Each is omitted on the **tier** rather than on a measured width, and each moves its tap to `more-info` in the same decision, so the tier does not take away the last way to operate the entity.
- **Recorded as conforming, on the measurement that shows it.** The **chip** and the **vertical pill group** were listed here as 34px and 38px parts, and the measurement corrects that reading: both numbers are `block-size`. The axis a one-cell-wide tier bounds is the **inline** one, and on that axis a chip is `padding-inline` plus its label and a pill group is a grid whose columns take their width from the container. Neither carries a fixed cross-axis size, so neither can overhang a region it is sized from — they are inside this rule and hold it, which is a different statement from being outside it.

The list is closed for the parts the design system names. It does not follow that no part will ever join them: anything given a fixed inline size at a tier that can be one cell wide is in scope by construction, and the three routes above are what it must be settled by.

#### Scenario: Vertical slider fits the narrowest tile it can occupy

- **GIVEN** a card at 1×3 (`tall`) on a 12-column grid whose container is 960px, so the tile is 63px wide, its content region 35px, and its height leaves the control a band well over 44px
- **WHEN** the vertical slider renders
- **THEN** the track is 35px thick — the region's width, not the token's 42px — and no part of it extends past the tile's edge
- **AND** the slider's own rendered length clears the 44px touch floor, so nothing is traded for the narrower track
- **WHEN** the same card renders where the content region is 42px or wider
- **THEN** the track is the token's 42px, unchanged
- **WHEN** LCARS is active, whose asymmetric inset leaves that tile a 19px content region
- **THEN** no slider renders at all — 19px is under the 24px floor — and the tile keeps its own primary action with the domain's controls behind the detail dialog
- **WHEN** a screen stored at 16 columns lays out a 43px tile under LCARS, leaving no content region
- **THEN** the same omission holds, and nothing renders at a width the user could not have hit
- **WHEN** the tile is short enough that the inset, the icon circle and the meta block leave the control a band under 44px — the grid's row height is derived from the container width, so a `tall` span does not guarantee a band
- **THEN** no slider renders either: the long-axis floor omits it exactly as the cross-axis floor does, rather than shrinking it to a track too short to drag.

#### Background slider placement

Besides its inline placements (horizontal on the row line, vertical filling a `tall` tile's middle — chosen by the tier layouts), the embedded slider MAY render as the **card surface itself** (change [0034](../../changes/0034-slider-placement.md); the option contract selecting it is [entity-cards options — common, shared slider placement](../entity-cards/options/common.md#shared-slider-placement-sliderplacement)):

- The track is the whole tile, **edge to edge** — no card padding between track and tile edge; the tile's own radius clips it. The card's icon, name, state and any other content overlay the track, laid out as the tier prescribes.
- The fill is the standard translucent domain tint over the standard track neutral, rising from the tile's leading edge. The direction derives from the **effective span** the renderer hands down — left→right when the span is wider than tall in cells, bottom→top otherwise, squares included (a bottom-up fill reads as a level) — never from measured pixels, so the control's orientation semantics are settled before first interaction from inputs the card already has. Because the fill is a tint over the card surface, overlaid text keeps its normal contrast on both the filled and unfilled regions — no scrim is needed; this is the tint pattern at tile scale, not content imagery.
- The 3px saturated leading edge remains, so the current level is readable as a line even at low fill.
- The slider keeps its accessible name on the element carrying `role="slider"`, exactly per the rule above; the tile's tap/hold actions remain reachable (the gesture contract lives with the option).
- Background placement composes with — and supersedes — the icon-only tile tint above when both are configured: the fill **is** the state surface, and its tint follows the same colour resolution.

### Size-adaptive layouts

Cards are freely resizable on the grid; content MUST adapt to the grid span rather than scale. Four layout tiers, derived from the item's **effective rendered span** — the responsive-scaled dimensions the grid actually lays out at the active breakpoint, not the stored dimensions (a stored 2×1 item collapsing to one effective cell on a narrow grid is `glance`, and the tier re-derives when the breakpoint changes):

| Tier     | Span            | Content                                                                         |
| -------- | --------------- | ------------------------------------------------------------------------------- |
| `glance` | 1×1             | Icon circle + name + state, stacked; whole tile is the primary action           |
| `row`    | ≥2 wide, 1 tall | Icon + meta in a row, plus the primary embedded control (slider, stepper, play) |
| `tall`   | 1 wide, ≥2 tall | Icon on top, vertical control (e.g. vertical dimmer), meta at bottom            |
| `full`   | ≥2×≥2           | Row layout plus secondary controls (mode pills, presets, graph, forecast)       |

- Every card MUST implement `glance` and `row`; `tall` and `full` are per-card (see per-card option docs under [entity-cards/options](../entity-cards/options/common.md)). **The camera card is the one whose tier response is behaviour rather than layout**, and it consumes the tier like every other card as of change [0021](../../changes/0021-camera-presentation-options.md): below `full` it mounts no stream element at all and stands a still thumbnail in its place, with the camera's own presentation layers — the name overlay, the live badge and the motion line — omitted. The UNIVERSAL display options still apply there: `hideName` hides the thumbnail tile's name exactly as it hides any other card's, leaving the image-only tile the camera's tier table requires to stay valid. Only the layers that need a feed to sit on go. The stream lifecycle on that path — including the lazy fullscreen mount — is owned by [camera-streaming](../camera-streaming/) and its presentation by [options/camera](../entity-cards/options/camera.md).
- A card MUST degrade gracefully: content that does not fit its tier MUST be omitted, never clipped or scrolled. What "does not fit" means for a part with a fixed size on the axis the tier is one cell wide is [cross-axis fit](#cross-axis-fit)'s.
- The states a card renders **instead of** itself — loading, error, unavailable — are tiles like any other and MUST take the tier of the card they stand in for: a 1×1 skeleton is a small tile, not a truncated large one.
- **Omitted from the layout is not omitted from the product.** Where a tier drops the information that identifies a state, or the only control that resolves it, both MUST stay reachable from the tile itself. Concretely for a `glance` error tile: the message MUST become the tile's accessible name; the tile MUST be a button; and pressing it MUST open the detail dialog carrying the full message and the `Retry` / `Dismiss` actions. A `title` tooltip MUST NOT be that mechanism — Liebe's primary surface is a wall tablet with no hover, and `title` reaches assistive technology inconsistently even where hover exists — and `Retry` MUST NOT simply be dropped, since it is the only way out of the state.
- The tier MUST be derived from grid dimensions; the legacy `size: small|medium|large` prop is superseded (migration is an implementation concern for the change documents, not this spec).
- **The effective rendered span is the stored span scaled to the column count actually in use**: `max(1, round(width × effectiveColumns / storedColumns))`, with the height untouched — the responsive breakpoints change how many columns a screen has, not how tall a row is. The one-cell floor is the grid's own: an item is never laid out narrower than a cell, however small the ratio gets.
- **The tier and the effective span are computed by the renderer and handed to the card.** A card MUST NOT derive its own tier and MUST NOT measure the DOM to infer its size. Both values travel together, because the tier is lossy — `row` covers 2×1 through N×1 — and a card contract may key on width past a tier boundary (a thermostat has room for independent low/high setpoints at ≥3 columns and not at 2). Where a card contract needs width-derived capacity (the weather forecast columns, [options/weather](../entity-cards/options/weather.md#forecast-presentation)), a second signal exists beside the pair: the pixel width **available to the card's content**. Its measurement ownership is explicit — the **shell** produces it by observing its own content box, the one component that owns that box; a single shared observation, not a per-card one. This is deliberately an observation rather than arithmetic on the renderer's laid-out width, because the content width moves without the grid moving (a theme setting a different `--liebe-card-padding`, LCARS's asymmetric frame) and because cards render in hosts with no grid renderer at all (the configuration preview, the sidebar widget, the workshop) — the shell renders in all of them, so the signal exists wherever a card does. Cards consume the signal exactly as they consume tier and span, and the standing prohibition is unchanged: a card itself never measures the DOM. Deriving a tier _from a grid layout_ happens in exactly one place; every other surface that renders a card supplies the pair explicitly, including surfaces with no grid at all:
  - A card's configuration preview MUST show the tier the card behind it is rendering, which means scaling the item's stored dimensions through the same breakpoint mapping. Stored dimensions alone would preview `row` for an item that renders `glance` on a four-column grid.
  - A fixed-size host — the sidebar's quick-controls widget — supplies the tier and span its own layout implies rather than inheriting a default.
- **A tier MUST NOT remove the last way to operate an entity.** `glance` drops embedded controls on the understanding that the whole tile is the primary action and a hold opens the detail dialog with that domain's controls; where those replacement controls do not exist for a card yet, the card MUST keep its minimal embedded control at that tier until they do. This is the same rule the `glance` error tile follows above, applied to ordinary states: a tile with nothing on it and nothing behind it is not a degraded layout, it is an entity the user can no longer reach.

#### Scenario: Resizing a light card

- **GIVEN** a light card at 2×1 showing icon, name/state, and a brightness slider
- **WHEN** the user resizes it to 1×1 in edit mode
- **THEN** the card re-renders in `glance` tier — icon over name/state, no slider — with the whole tile toggling the light.

#### Scenario: Boundary span

- **GIVEN** a card whose effective rendered span is 3×1
- **WHEN** the tier is derived
- **THEN** it is `row` — ≥2 wide and 1 tall — as 1×2 is `tall` and 2×2 is `full`.

### Motion

- State-driven color/background changes MUST transition ~280ms ease-out.
- Press feedback ≤100ms. Overlay/sheet entrances ≤250ms.
- All non-essential animation MUST be disabled under `prefers-reduced-motion: reduce`.

## Design

Token layering (base → theme → user), application mechanism, and shadow-DOM constraints are owned by the [theming spec](../theming/). Radix integration approach:

- Chrome and edit-mode UI (dialogs, selects, config forms) remain stock Radix Themes components.
- Card anatomy parts are custom components styled by tokens; behavior-heavy controls (slider) build on unstyled Radix primitives (`@radix-ui/react-slider`) rather than bending `@radix-ui/themes` styled components.
- The card surface uses a plain token-styled element (not `Card variant="classic"`, whose inset borders fight the flat look).
- Tokens are declared on the **Radix theme root** (`.radix-themes`), not on the shadow host or the React container above it: Radix declares `--color-*`, `--gray-*` and `--default-font-family` there, and a `var()` inside a custom property substitutes at the element that declares it, so aliasing from any higher element resolves to nothing. It is also the element Radix marks `.dark`/`.light`, which is what makes the appearance-conditional tokens flip with Radix's own signal. **Consequence for the theme and user layers:** their token overrides MUST land on this same element — a derived companion (`-tint`) only re-derives where its base is overridden on the same element, so an override on a descendant would leave tint and text behind on the old hue.
- Per-domain coloring MUST be applied by consuming the `--liebe-c-*` triplets, never by passing Radix per-instance `color` props or referencing Radix scale variables at the point of use. Those bypass the token contract: a component coloured by a Radix prop keeps its original hue when LCARS or user CSS remaps the triplet, silently breaking the remapping promise the token contract makes. The Radix scales remain the _source_ of the default theme's token values — the indirection through `--liebe-c-*` is what makes them themeable. The single Theme `accentColor` is not used for domain state.

## Constraints

- Radix UI Themes stays the component library; customization order remains props → token adjustments → primitives + tokens → minimal overrides (project AGENTS.md).
- The panel renders inside Home Assistant's shadow DOM; token definitions and theme styles must be injected into the shadow root (see [panel-lifecycle](../panel-lifecycle/) and [theming](../theming/)).
- No custom z-index values beyond `auto`/`0`/`-1`; overlays use portals. **Standing exception:** the camera in-place fullscreen (`CAMERA_FULLSCREEN_Z_INDEX`, [camera-streaming](../camera-streaming/), change 0008) — the stream node cannot portal without reconnecting, so it lifts above Home Assistant's chrome by z-index by design; that contract is unaffected by this rule.
- Touch-first: 44px minimum targets, `-webkit-tap-highlight-color` managed centrally.
- All gates in [architecture — Testing & Quality Conventions](../architecture/index.md#testing--quality-conventions) apply to implementing changes.

## Open Questions

- ~~**Radix alias fidelity.**~~ Answered by change [0010](../../changes/0010-design-tokens-and-anatomy.md) PR 1, compared side by side in the workshop's `Design System/Tokens` → _Alias fidelity_ story: no literals are pinned. In light Radix resolves both `--color-background` and `--color-panel-solid` to plain white, which would flatten ground and card into one surface, so the surface tokens alias appearance-specific gray-scale **steps** instead of the semantic pair — see the note under the surface token table.
- ~~**Sparkline data source.**~~ Settled and shipped: the sparkline consumes [`useEntityHistory`](../entity-state/index.md#entity-history), which owns the contract (change [0015](../../changes/0015-history-and-forecast-data.md) PR 1). Wiring cards to it is a follow-up (0018/0020).
- ~~**Legacy `size` prop migration.**~~ Audited and closed by change [0011](../../changes/0011-layout-tiers.md) PR 3, recorded in [dashboard-config — Grid Item Mutations](../dashboard-config/index.md#grid-item-mutations): no `size` value was ever stored. The prop was computed at render time from the item's already-persisted `width`/`height`, so there is nothing to map and no configuration migrates. The tier is likewise derived and never persisted.

## References

- Related specs: [theming](../theming/), [entity-cards](../entity-cards/), [storybook](../storybook/), [grid-layout](../grid-layout/)
- Radix styling guidance: https://www.radix-ui.com/themes/docs/overview/styling

## Changelog

| Date       | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Document                                                  |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| 2026-07-25 | Initial spec created (target design system, not yet implemented)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | —                                                         |
| 2026-07-26 | Token contract implemented (dark + light, Radix-aliased); "Radix alias fidelity" answered — no literals pinned, surface tokens alias appearance-specific gray steps; token declaration root documented                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [0010](../../changes/0010-design-tokens-and-anatomy.md)   |
| 2026-07-26 | Anatomy parts implemented (icon circle, meta block, pills, chips, value, sparkline); part attributes (`data-domain`/`data-active`/`data-color`) documented; label contrast rule added for text on a tint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | [0010](../../changes/0010-design-tokens-and-anatomy.md)   |
| 2026-07-26 | Embedded slider implemented on the unstyled Radix primitive (both orientations, readout in the track); rule added that the accessible name belongs on the element carrying `role="slider"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | [0010](../../changes/0010-design-tokens-and-anatomy.md)   |
| 2026-07-26 | Card shell implemented as the token-styled `liebe-card` tile (flat dark / small shadow light, state treatments as `data-*` attributes, press feedback and reduced motion); every card's shell, icon circle, name and state line moved onto the anatomy and their inline visual declarations removed; `Card variant="classic"` retired from the shell. Layout tiers remain 0011's.                                                                                                                                                                                                                                                                                                                                                                             | [0010](../../changes/0010-design-tokens-and-anatomy.md)   |
| 2026-07-26 | Recorded that the base layer's derived `-text` default is unusable as rendered, so every theme MUST pin all ten `-text` tokens (#197, #210)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | [0013](../../changes/0013-built-in-themes.md)             |
| 2026-07-27 | Status qualified with the five defects open against implemented rules (#191, #197, #204, #210, #215); layout tiers still unshipped and `data-tier` still unstamped, unchanged by 0013                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | [0013](../../changes/0013-built-in-themes.md)             |
| 2026-07-27 | "Sparkline data source" open question closed — `useEntityHistory` shipped as the source; card wiring remains 0018/0020                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [0015](../../changes/0015-history-and-forecast-data.md)   |
| 2026-07-27 | Tier derivation implemented: the renderer computes `glance`/`row`/`tall`/`full` from the item's effective rendered span and passes tier and span to cards, `data-tier` is stamped on every shell, and the legacy `size` prop is retired. Per-tier content is still per-card work                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | [0011](../../changes/0011-layout-tiers.md)                |
| 2026-07-27 | Placeholder and error tiles brought under the tier rules, and the `glance` error tile's contract recorded here rather than in a change doc: accessible name carries the message, the tile is a button onto the detail dialog, `title` is not an acceptable substitute, `Retry` is never dropped. Two tier-floor tokens (`--liebe-card-min-height-row`, `--liebe-card-min-height-tall`) added to the geometry contract                                                                                                                                                                                                                                                                                                                                         | [0011](../../changes/0011-layout-tiers.md)                |
| 2026-07-27 | Tier layouts implemented across the existing cards — light, cover, fan and weather (all four variants) drop to icon + name + state at `glance`, carry their primary control at `row`, stand it up at `tall`, and add secondary content at `full`; the thermostat keeps a setpoint stepper at `glance` under the no-operability-regression rule until change 0017 registers its dialog controls. `PillGroup` gained a vertical orientation for `tall`. The camera keeps its exemption: stamped, not consuming                                                                                                                                                                                                                                                  | [0011](../../changes/0011-layout-tiers.md)                |
| 2026-07-27 | "Legacy `size` prop migration" open question closed by audit — the prop was never persisted, so no stored configuration migrates ([dashboard-config](../dashboard-config/index.md#grid-item-mutations))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | [0011](../../changes/0011-layout-tiers.md)                |
| 2026-07-27 | Rules 0011 had been carrying in its change document moved here, where behaviour belongs: the effective-span scaling formula, the renderer-computes-and-hands-down rule (with what the configuration preview and a fixed-size host must supply), the invariant that a tier never removes the last way to operate an entity, the camera's standing exemption, and the boundary-span scenario                                                                                                                                                                                                                                                                                                                                                                    | [0011](../../changes/0011-layout-tiers.md)                |
| 2026-07-27 | The camera stops being the tier exemption: it consumes `data-tier` like every other card, degrading below 2×2 to a still thumbnail with no stream element mounted, and `--liebe-media-bg` joins the surface tokens so the well a picture sits in is reachable from the theme layer instead of pinned to a Radix scale at the point of use                                                                                                                                                                                                                                                                                                                                                                                                                     | [0021](../../changes/0021-camera-presentation-options.md) |
| 2026-07-29 | Slider anatomy tightened with the rule the shipped defect exposed: the fill MUST cover the track's full cross-axis in both orientations, and a vertical slider MUST be centred in its hosting region                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [0028](../../changes/0028-slider-rendering-fixes.md)      |
| 2026-07-29 | Content-imagery legibility rule added: text over artwork MUST sit on a scrim holding the 4.5:1 floor — shadows alone are not a scrim (#215); the media backdrop is the reference treatment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | [0030](../../changes/0030-weather-forecast-legibility.md) |
| 2026-07-29 | Icon-only tile exception recorded: a card in icon-only presentation carries the active/inactive tint pattern on the tile itself (level-modulated for level-bearing domains) — one of the two exceptions to the neutral-tile rule, beside the background slider placement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | [0033](../../changes/0033-icon-only-cards.md)             |
| 2026-07-29 | Background slider placement specified: the embedded slider may render as the card surface itself, edge to edge, with the tint fill as the state surface                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | [0034](../../changes/0034-slider-placement.md)            |
| 2026-07-29 | Graph geometry joined the token contract: `--liebe-graph-height-inline` and `--liebe-graph-height-dialog` name the two fixed graph heights, and the `full`-tier card graph is recorded as deliberately unfixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | [0031](../../changes/0031-sensor-graph-fill.md)           |
| 2026-07-30 | Content-imagery scrim rule met on both its consumers, and two mechanisms folded into it that the observable phrasing does not imply: the scrim paints beneath the text, and the foreground override reaches the parts rather than only the tile. Weather artwork worst 5.18:1 (was 1.01:1), media backdrop 7.11:1 against a white cover (was 1.00:1)                                                                                                                                                                                                                                                                                                                                                                                                          | [0030](../../changes/0030-weather-forecast-legibility.md) |
| 2026-07-30 | Default theme's `-text` companions pinned per appearance: step 12 in light for amber, orange and teal, step 11 for the other seven and for all ten in dark. A `-text` pin MUST now clear the 4.5:1 text floor in each appearance separately, since a Radix step 11 is calibrated against its own scale rather than a neutral                                                                                                                                                                                                                                                                                                                                                                                                                                  | [0035](../../changes/0035-light-appearance-contrast.md)   |
| 2026-07-30 | The 44px touch-target floor is enforced on Radix controls again: the vendored sheets moved below the baseline layer, where their class selectors no longer outrank the floor's element selectors                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | [0036](../../changes/0036-theming-contract-gaps.md)       |
| 2026-07-30 | Active-state pattern's glyph step made per appearance: the glyph takes the domain's `-text` token in light and its base in dark, because a base-step glyph on a 20% tint of itself over the light card cannot reach 3:1 whatever the hue (1.40:1 amber, 2.50:1 green) and neither of the pattern's other two knobs moves it                                                                                                                                                                                                                                                                                                                                                                                                                                   | [0035](../../changes/0035-light-appearance-contrast.md)   |
| 2026-07-30 | Cross-axis fit added to card anatomy: a part may not lean on the tile's inset to absorb an overhang, a control at a one-cell-wide tier must be cross-axis flexible down to a 24px floor (WCAG 2.2 SC 2.5.8) and omitted below it, sizing down is legitimate only where the control's own long axis clears 44px as rendered, and anything else is omitted in favour of a control that fits. The region has no lower bound — a 16-column screen at 960px gives a 43px tile, which LCARS's inset leaves with no content region at all. `--liebe-control-height` re-pinned as a preferred maximum rather than a fixed thickness — same name, same 42px default                                                                                                    | [0042](../../changes/0042-tall-tile-control-geometry.md)  |
| 2026-07-30 | Content-width signal implemented: the card shell observes its own content box once and publishes the pixel width through `useCardContentWidth`, beside the tier and span it already hands down. The signal distinguishes an unobserved width from a measured zero — a consumer omits fixed-minimum-width content on the second and falls back to its width-blind contract on the first. The card-never-measures-the-DOM prohibition is unchanged                                                                                                                                                                                                                                                                                                              | [0030](../../changes/0030-weather-forecast-legibility.md) |
| 2026-07-30 | Part role tokens added to the token contract: `--liebe-part-color` publishes the domain colour the part in hand resolved to (live bulb colour included), and `--liebe-part-label` / `--liebe-part-label-active` the colour of a label on a pill or chip. The label pair are overrides, declared on the root like any other token and defaulted by a `var()` fallback where they are used rather than by a value on the root; the hue is published to be read, since re-declaring it per part puts it out of reach of a root-level override. All three default to what the base layer already rendered, so no theme changes until one sets them; a theme that recolours a label MUST measure it at 4.5:1 against the fill it recoloured it for, per appearance | [0036](../../changes/0036-theming-contract-gaps.md)       |
| 2026-07-30 | The `button-name` audit closed: the `input_select` trigger and the `input_boolean` own-tile switch took accessible names, so the anatomy's naming rule is held on every control the workshop renders. Re-run over all 645 stories in both appearances, axe reports zero `button-name` nodes, down from 32 on 15 stories                                                                                                                                                                                                                                                                                                                                                                                                                                       | [0035](../../changes/0035-light-appearance-contrast.md)   |
| 2026-07-30 | Icon-only tile exception implemented: the tile carries the tint as a wash over the card surface, resolved through the same triplet (bulb colour included) and modulated by the entity's level, and the icon circle drops its own tint there so the glyph keeps the single-tint ground the pattern is calibrated against — except the person card's initials avatar, whose disc IS its anchor                                                                                                                                                                                                                                                                                                                                                                  | [0033](../../changes/0033-icon-only-cards.md)             |
| 2026-07-30 | A live hue now colours the surface and the solid roles only: a bulb's own RGB under `useLightColor` and a person's identity colour supply the tint and the saturated solid, while the glyph and the text role take the neutral foreground, so a foreground is never drawn on a 20% veil of itself. Measured from decoded pixels on Default and Liquid Glass in both appearances: the bulb glyph 1.01:1 → 5.95:1 worst against a 3:1 floor, the avatar's initials 2.19:1 → 10.53:1 worst against the 4.5:1 they answer to as text. LCARS is unchanged and is now the outstanding item, since it declares the glyph's colour itself                                                                                                                             | [0035](../../changes/0035-light-appearance-contrast.md)   |
| 2026-07-30 | Cross-axis fit met by the `tall` vertical slider on every card that renders one (light, cover, fan, `input_number`): the track takes `--liebe-control-height` where the content region affords it and the region's width where it does not, and is omitted below either floor rather than narrowed or shortened past it. The long-axis capacity is now a published signal — the design system's own layout component observes the control band through the same shared observation the content width comes from, since the tile's height less the inset, the icon circle, the meta block and the gaps is not derivable from tier and span                                                                                                                     | [0042](../../changes/0042-tall-tile-control-geometry.md)  |
| 2026-07-30 | Cross-axis fit met by every part the rule names, by three routes: the icon circle made flexible (`min(--liebe-icon-circle, 100%)` with an aspect ratio, since a symmetric inset cancels and the circle is clipped only where the tile itself is narrower than it); `input_text`'s field and `input_datetime`'s inputs omitted at `tall` with their tap moved to `more-info`, being bounded by their own content on the axis that tier is one cell wide; and the chip and the vertical pill group recorded as conforming — their 34px and 38px are `block-size`, and the axis a one-cell-wide tier bounds is the inline one, which both take from their content                                                                                                | [0042](../../changes/0042-tall-tile-control-geometry.md)  |
