**/build
**/public
coverage/
storybook-static/
pnpm-lock.yaml
routeTree.gen.ts
.tailscale/

# Home Assistant e2e runtime state (only configuration.yaml is committed).
ha/config/**
!ha/config/configuration.yaml

# Local throwaway design mockups (untracked, see .gitignore)
mockups/

# Lint fixtures written and deleted by src/__tests__/effectHookLintGate.test.ts.
# Ignored so a run killed mid-flight cannot leave the format gate failing on
# files nobody wrote by hand.
src/__lint-fixture__/

# Agent worktrees checked out inside the repo (see .gitignore). Scoped to
# worktrees/ so tracked .claude/ files stay formatted like the rest of the repo.
.claude/worktrees/
